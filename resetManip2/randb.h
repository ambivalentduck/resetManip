#include <time.h>
#include <stdlib.h>

void init_randb();
double randb(double lower, double upper);
int randp(double mean);
double randd();
int randint(int lower, int upper);
void randperm(int * n, int i=-1);
